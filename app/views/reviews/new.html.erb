<div class="margin-navbar"></div>

<div class="container-show-trip">
  <div class="m-3 mx-5 text-center">
    <h2><%= @trip.title %></h2>
  </div>

  <div class="mt-5 mx-5">
    <h3>Write Your Review</h3>
  </div>

  <div class="mt-3 mx-5 myborder">
    <div class="m-5">
      <%= simple_form_for @review, url: url_for(:action => 'create', :controller => 'reviews'), html: { id: "review-form" } do |f| %>
          <%= f.input :content %>
          <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience", input_html: {data: {controller: "star-rating"}} %>
        <%= f.submit "Submit review", class: "btn-general-pink mt-3" %>
      <% end %>
    </div>
  </div>
</div>
